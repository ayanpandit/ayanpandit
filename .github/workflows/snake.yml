name: Generate Snake Animationname: Generate Snake Animation



on:on:

  schedule:  schedule:

    - cron: "0 */12 * * *"    - cron: "0 */12 * * *"

  workflow_dispatch:  workflow_dispatch:

  push:  push:

    branches:    branches:

    - main    - main



jobs:jobs:

  generate:  generate:

    permissions:    runs-on: ubuntu-latest

      contents: write    timeout-minutes: 10

    runs-on: ubuntu-latest    

    timeout-minutes: 10    steps:

          - name: Generate snake animation

    steps:        uses: Platane/snk/svg-only@v3

      - name: Generate snake animation        with:

        uses: Platane/snk/svg-only@v3          github_user_name: ayanpandit

        with:          outputs: |

          github_user_name: ayanpandit            dist/github-contribution-grid-snake.svg

          outputs: |            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

            dist/github-contribution-grid-snake.svg            

            dist/github-contribution-grid-snake-dark.svg?palette=github-dark      - name: Push to output branch

                    uses: crazy-max/ghaction-github-pages@v3.1.0

      - name: Push to output branch        with:

        uses: crazy-max/ghaction-github-pages@v3.1.0          target_branch: output

        with:          build_dir: dist

          target_branch: output        env:

          build_dir: dist          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

        env:```

          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

4. Click **"Commit changes"**

### **Step 4: Enable Permissions**

1. Go to your repo **Settings**
2. Scroll down to **"Actions"** → **"General"**
3. Under **"Workflow permissions"**, select **"Read and write permissions"**
4. Click **"Save"**

### **Step 5: Run the Workflow**

1. Go to the **"Actions"** tab in your repo
2. Click **"Generate Snake Animation"** (left sidebar)
3. Click **"Run workflow"** → **"Run workflow"**
4. Wait 1-2 minutes
5. Refresh the page - you should see a green checkmark ✅

### **Step 6: Add the README**

1. Go back to your repo main page
2. Click on **"README.md"** file
3. Click the **pencil icon** (Edit)
4. **Delete everything** and paste the entire README code I gave you
5. Click **"Commit changes"**

### **Final Structure Should Look Like:**
```
ayanpandit/ayanpandit/
├── .github/
│   └── workflows/
│       └── snake.yml
├── README.md